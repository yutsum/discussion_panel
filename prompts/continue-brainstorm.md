Use the `brainstorm-moderator` skill.

Continue this brainstorming session from the prior transcript.

Topic:
`<topic>`

Prior transcript:
`<paste prior transcript or summary>`

Instructions:
- continue with one more discussion round
- let each persona react to at least one other persona
- let each persona speak in 2-4 sentences
- let each persona cite prior speakers by name when relevant
- treat each persona as isolated; only transcript, shared brief, persona instructions, and that persona's own scratchpad are visible to it
- let personas use the shared brief only when needed; avoid repetitive evidence-labeling phrases unless the factual basis itself is contested
- run personas sequentially within the round and extend a round-in-progress transcript after each turn so later speakers can react to earlier speakers from the same round
- across rounds, prefer reusing the same agent for the same participant while sending fresh explicit context each time
- append the new round to the same transcript file
- stop and let the user optionally comment, continue silently, add constraints, or request a separate summary
